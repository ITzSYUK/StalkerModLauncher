<Window x:Class="StalkerModLauncher.Views.ProfileHealthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Состояние профиля"
        Width="900"
        Height="650"
        MinWidth="720"
        MinHeight="480"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        SourceInitialized="Window_OnSourceInitialized">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Состояние профиля"
                       FontSize="24"
                       FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ProfileName}"
                       Foreground="{StaticResource AccentBrush}"
                       FontSize="15"
                       Margin="0,2,0,0"/>
        </StackPanel>

        <Border Grid.Row="1"
                Background="{StaticResource CommandPanelBrush}"
                BorderBrush="{StaticResource StrokeBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,14,0,12">
            <TextBlock Text="{Binding Summary}"
                       FontSize="14"
                       FontWeight="SemiBold"/>
        </Border>

        <ListBox Grid.Row="2"
                 ItemsSource="{Binding Checks}"
                 BorderThickness="1"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="160"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="8"
                                Height="8"
                                CornerRadius="4"
                                VerticalAlignment="Top"
                                Margin="0,7,0,0">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="{StaticResource AccentGreenBrush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Warning">
                                            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Status}" Value="Error">
                                            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <TextBlock Grid.Column="1"
                                   Text="{Binding Title}"
                                   FontWeight="SemiBold"
                                   Margin="8,0,12,0"/>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding Details}"
                                   Foreground="{StaticResource MutedTextBrush}"
                                   TextWrapping="Wrap"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <WrapPanel Grid.Row="3"
                   HorizontalAlignment="Right"
                   Margin="0,14,0,0">
            <Button Content="Обновить"
                    Command="{Binding RefreshCommand}"
                    Margin="0,0,8,0"/>
            <Button Content="Workspace"
                    Command="{Binding OpenWorkspaceCommand}"
                    Margin="0,0,8,0"/>
            <Button Content="Сохранения"
                    Command="{Binding OpenSavesCommand}"
                    Margin="0,0,8,0"/>
            <Button Content="Последний лог"
                    Command="{Binding OpenLatestLogCommand}"
                    Margin="0,0,8,0"/>
            <Button Content="Crash dump"
                    Command="{Binding OpenCrashDumpCommand}"
                    Margin="0,0,8,0"/>
            <Button Content="Копировать отчет"
                    Command="{Binding CopyReportCommand}"/>
        </WrapPanel>
    </Grid>
</Window>
