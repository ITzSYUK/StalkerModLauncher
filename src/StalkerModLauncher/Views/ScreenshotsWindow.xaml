<Window x:Class="StalkerModLauncher.Views.ScreenshotsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Скриншоты"
        Width="1100"
        Height="700"
        MinWidth="800"
        MinHeight="500"
        Background="{StaticResource WindowBackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        SourceInitialized="ScreenshotsWindow_OnSourceInitialized"
        KeyDown="ScreenshotsWindow_OnKeyDown">
    <Grid Margin="16">
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <ControlTemplate x:Key="IconButtonTemplate" TargetType="Button">
                <Border Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="4">
                    <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                </Border>
            </ControlTemplate>
        </Grid.Resources>

        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding ShowThumbnailGrid, Converter={StaticResource BoolToVis}}">
            <ItemsControl ItemsSource="{Binding Screenshots}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="6"
                                Background="{StaticResource FieldBackgroundBrush}"
                                BorderBrush="{StaticResource StrokeBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Cursor="Hand">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
                                              Command="{Binding DataContext.OpenScreenshotCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                              CommandParameter="{Binding}"/>
                            </Border.InputBindings>
                            <Image Source="{Binding Source}"
                                   Width="240"
                                   Stretch="Uniform">
                                <Image.Style>
                                    <Style TargetType="Image">
                                        <Setter Property="Opacity" Value="0.9"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Image.Style>
                            </Image>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid Visibility="{Binding ShowFullScreenView, Converter={StaticResource BoolToVis}}"
              PreviewMouseWheel="FullScreenGrid_PreviewMouseWheel">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="◀"
                    Command="{Binding GoPreviousCommand}"
                    Width="40"
                    Height="40"
                    FontSize="20"
                    Margin="0,0,12,0"
                    VerticalAlignment="Center"
                    BorderThickness="0"
                    Template="{StaticResource IconButtonTemplate}"
                    Background="Transparent"
                    IsEnabled="{Binding CanGoPrevious}"/>

            <Image Grid.Column="1"
                   Source="{Binding SelectedScreenshot}"
                   Stretch="Uniform"
                   Margin="0,0,0,0"/>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0"
                        Content="✕"
                        Command="{Binding CloseFullScreenCommand}"
                        Width="28"
                        Height="28"
                        FontSize="14"
                        Margin="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Padding="0"
                        BorderThickness="0"
                        Template="{StaticResource IconButtonTemplate}"
                        Background="Transparent"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"/>
                <Button Grid.Row="1"
                        Content="▶"
                        Command="{Binding GoNextCommand}"
                        Width="40"
                        Height="40"
                        FontSize="20"
                        Margin="12,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        BorderThickness="0"
                        Template="{StaticResource IconButtonTemplate}"
                        Background="Transparent"
                        IsEnabled="{Binding CanGoNext}"/>
            </Grid>
        </Grid>

        <TextBlock Text="Скриншоты не найдены."
                   Foreground="{StaticResource MutedTextBrush}"
                   FontSize="16"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding HasNoScreenshots, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</Window>
